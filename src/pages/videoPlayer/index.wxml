<view class="w-screen h-screen">
  <view class="back" bind:tap="onNavigateBack">返回</view>
  <video id="video" class="w-full h-full" show-fullscreen-btn="{{false}}" poster="{{selectedVideo.data.thumbnail}}" src="{{selectedVideo.data.src}}" controls="ture"></video>
</view>